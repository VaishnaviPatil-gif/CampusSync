# CampusSync Backend v2 Shared Layer

This directory contains schemas, constants, and utilities that are shared across both Flask (Python) and Express (Node.js) services.

- `constants/` — Shared status codes, role names, and message formats.
- `schemas/` — API request schemas and models.
